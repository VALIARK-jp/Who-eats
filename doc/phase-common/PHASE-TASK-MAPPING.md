# タスクのフェーズ分け（固定版）

## 前提
- Phase 1: モニター/自チーム検証で使える実装を揃える（期限: 5/22）
- Phase 2: 中間発表向けのデザイン微調整・見せ方強化（期限: 6/5）
- Phase 3: 最終発表向け改善（期限: 7月下旬）
  - 現時点ではタスク化しない（実利用フィードバック起点で後から作成）

## Phase 1（5/22）必須
- `doc/phase1/TASKS.md` の `TASK-P1-01` 〜 `TASK-P1-21`
- 優先順は `P0 -> P1 -> P2` の順で着手
- **共通コンポーネントは作らない**（Phase2で導入）

## Phase 2（6/5）対象
- `doc/phase2/TASKS.md` の `TASK-P2-01` 〜 `TASK-P2-19`
- 優先順は `P0 -> P1 -> P2` の順で着手
- **共通コンポーネントは Phase2 から導入**（`TASK-P2-07`）

## Phase 3方針
- いまは作らない
- フィードバック起点でIssue化してから着手

## 運用ルール
- DB変更（DDL/migration）はPMのみ
- メンバーは機能実装（API/UI/テスト/バグ修正）に集中
- フェーズ跨ぎの移動はPM判断

