# フェーズ別タスク配置

- `doc/phase-common/PHASE-TASK-MAPPING.md` : フェーズ全体方針
- `doc/phase1/TASKS.md` : Phase 1 実装タスク
- `doc/phase2/TASKS.md` : Phase 2 実装タスク
- `doc/pm-only/` : PM専用タスク（認証/DB/API固定/配布前前提）

Phase 3 は現時点でタスク未作成（フィードバック起点で後日作成）。

**コンポーネント**: Phase1では新設しない。Phase2の `TASK-P2-07` から導入する。

